<Window x:Class="UI.EditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="EditProductWindow" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="NameProduct" Text ="Название Продукта" HorizontalAlignment="Left" Margin="10,126,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="111"/>
        <TextBox x:Name="DesriptionVar" Text ="Описание вариации" HorizontalAlignment="Left" Margin="194,229,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="112"/>
        <TextBlock HorizontalAlignment="Left" Margin="194,74,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="28" Width="128"><Run Language="ru-ru" Text="Добавление вариации"/></TextBlock>
        <TextBlock HorizontalAlignment="Left" Margin="15,70,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="31" Width="87"><Run Language="ru-ru" Text="Добавления продукта"/></TextBlock>
        <TextBlock x:Name="PathTextBox" HorizontalAlignment="Left" Margin="193,120,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="202" Height="66" RenderTransformOrigin="0.5,0.244"><Run Text="Path Фотографии"/><Run Language="ru-ru"/></TextBlock>
        <Button x:Name="Path" Content="Фотография" HorizontalAlignment="Left" Margin="195,191,0,0" VerticalAlignment="Top" Width="111" Click="path_Click"/>
        <TextBox x:Name="CostVariation" HorizontalAlignment="Left" Margin="193,284,0,0" TextWrapping="Wrap" Text="Цена" VerticalAlignment="Top" Width="113" />
        <Button x:Name="CreateButton" Content="Создать" HorizontalAlignment="Left" Margin="542,229,0,0" VerticalAlignment="Top" Click="EditProduct_Click"/>
        <Button Content="Отмена" HorizontalAlignment="Left" Margin="481,229,0,0" VerticalAlignment="Top" Width="48"/>
        <ComboBox x:Name="CategoryComboBox" Width="92" Height="40" Margin="15,186,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        <ListBox x:Name="ListBoxVariation" Margin="400,30,10,225" ItemsSource="{Binding ProductVariation}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E0E0E0" Margin="5" Padding="10" Background="White" CornerRadius="10,10,10,10" BorderThickness="1,1,1,1">
                        <StackPanel Orientation="Horizontal">
                            <!-- Изображение товара -->
                            <Image Source="{Binding ImagePath}" 
                        Width="200" Height="100" 
                        Stretch="Uniform"
                        Margin="0,0,10,0"/>

                            <StackPanel>
                                <TextBlock Text="{Binding Product.Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Description, StringFormat={}Описание: {0}}"/>
                                <TextBlock x:Name="LIstBoxVariation" Text="{Binding Price, StringFormat={}Общая стоимость: {0}₽}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
