<Window x:Class="UI.ProductCatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Каталог товаров" Height="600" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="800"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="В корзину" Grid.Row="0" Grid.Column="1" Margin="10" Click="Button_Click"/>

        <ListView x:Name="ProductsListBox" 
                  ItemsSource="{Binding Products}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Grid.Row="1" Grid.ColumnSpan="2"
                  SelectionChanged="ProductsListBox_SelectionChanged">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" 
                               Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" 
                            Margin="10" Background="White"
                            Width="150" Height="200">
                        <StackPanel>
                            <!-- Изображение -->
                            <Border Height="100" Background="#F5F5F5">
                                <Image Source="{Binding MainImagePath}" 
                                       Stretch="Uniform" 
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       MaxWidth="130" MaxHeight="90"/>
                            </Border>

                            <!-- Информация -->
                            <StackPanel Margin="10,5">
                                <TextBlock Text="{Binding Name}" 
                                           FontWeight="SemiBold" 
                                           TextWrapping="Wrap"
                                           Height="40"
                                           Margin="0,0,0,5"
                                           TextAlignment="Center"/>

                                <TextBlock Text="{Binding Pricevar, StringFormat={}{0:N0}₽}" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Foreground="#E53935"
                                           TextAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Window>