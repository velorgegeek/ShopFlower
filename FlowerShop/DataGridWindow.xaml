<Window x:Class="UI.DataGridWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="DataGridWindow" Height="450" Width="800">

    <TabControl>
        <TabItem Header="Продукты">
            <Grid Background="#FFE5E5E5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="122*"/>
                    <ColumnDefinition Width="135*"/>
                    <ColumnDefinition Width="170*"/>
                    <ColumnDefinition Width="367*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Изменил: Auto вместо 17* -->
                    <RowDefinition Height="*"/>
                    <!-- Изменил: * вместо 186* -->
                </Grid.RowDefinitions>

                <Button x:Name="AddProduct" Content="Добавить продукт" Grid.Row="0" Grid.Column="0" Click="AddProduct_Click" Margin="5,5,5,5"/>
                <Button x:Name="EditProduct" Content="Редактировать продукт" Grid.Row="0" Grid.Column="2" Click="EditProduct_Click" Margin="0,5,0,5" HorizontalAlignment="Center" Width="164"/>
                <Button x:Name="AddVariation" Content="Добавить вариацию" Grid.Row="0" Grid.Column="1" Margin="5,5,5,5" Click="AddVariation_Click"/>

                <!-- DataGrid с разворачиваемым ListBox -->
                <DataGrid IsReadOnly="True" x:Name="ProductsDataGrid" Margin="0,4,0,0" Grid.ColumnSpan="4" Grid.Row="1" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding id}"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}"/>
                        <DataGridTextColumn Header="Категория" Binding="{Binding category}"/>
                    </DataGrid.Columns>
                    <!-- Шаблон для развернутых деталей с ListBox -->
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <!-- Border с фиксированной высотой -->
                            <Border Background="#FFF8F8F8" 
                                    BorderBrush="#FFCCCCCC" 
                                    BorderThickness="0,1,0,0"
                                    Padding="10"
                                    Height="250">
                                <!-- Фиксированная высота -->

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Заголовок -->
                                    <TextBlock Text="Вариации продукта:" 
                                               FontWeight="Bold" 
                                               FontSize="14"
                                               Margin="0,0,0,10"
                                               Grid.Row="0"/>

                                    <!-- ListBox с вариациями -->
                                    <ListBox x:Name="ListVariation"
                                             ItemsSource="{Binding Variations}"
                                             Background="Transparent"
                                             Grid.Row="1"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                        <!-- Панель для горизонтального расположения карточек -->
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" 
                                                           ItemWidth="180" 
                                                           ItemHeight="200"/>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>

                                        <!-- Убираем стандартный фон при выборе -->
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <ContentPresenter/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>

                                        <!-- Шаблон элемента ListBox -->
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#E0E0E0" 
                                                        BorderThickness="1" 
                                                        CornerRadius="5"
                                                        Background="White"
                                                        Margin="5"
                                                        Width="170" 
                                                        Height="190">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="100"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <!-- Изображение -->
                                                        <Border Grid.Row="0" 
                                                                Background="#F5F5F5"
                                                                CornerRadius="3">
                                                            <Image Source="{Binding ImagePath}" 
                                                                   Stretch="Uniform"
                                                                   MaxWidth="150"
                                                                   MaxHeight="80"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"/>
                                                        </Border>

                                                        <!-- Информация -->
                                                        <StackPanel Grid.Row="1" 
                                                                    Margin="10,5">
                                                            <TextBlock Text="{Binding Product.Name}" 
                                                                       FontWeight="Bold"
                                                                       TextWrapping="Wrap"
                                                                       MaxHeight="20"/>

                                                            <StackPanel Margin="0,5,0,0">
                                                                <TextBlock Text="{Binding Description, StringFormat='Описание: {0}'}"
                                                                           FontSize="11"/>
                                                                <TextBlock Text="{Binding Price, StringFormat='Цена: {0}₽'}"
                                                                           FontSize="11"
                                                                           Margin="0,2,0,0"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </Grid>
        </TabItem>

        <TabItem Header="Покупки">
            <Grid Background="#FFE5E5E5">
                <DataGrid x:Name="SaleDataGrid" Margin="5" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding Id}"/>
                        <DataGridTextColumn Header="Ид Юзера" Binding="{Binding UserId}"/>
                        <DataGridTextColumn Header="Дата заказа" Binding="{Binding DateCreate}"/>
                        <DataGridTextColumn Header="Общая стоимость" Binding="{Binding amount}"/>
                        <DataGridTextColumn Header="Кол-во товаров" Binding="{Binding CountProducts}"/>
                    </DataGrid.Columns>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Background="#FFF8F8F8" 
                            BorderBrush="#FFCCCCCC" 
                            BorderThickness="0,1,0,0"
                            Padding="10"
                            Height="250">
                                <!-- Фиксированная высота -->

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Продукты в покупке:" 
                                       FontWeight="Bold" 
                                       FontSize="14"
                                       Margin="0,0,0,10"
                                       Grid.Row="0"/>

                                    <!-- ListBox с вариациями -->
                                    <ListBox x:Name="ListProduct"
                                     ItemsSource="{Binding Products}"
                                     Background="Transparent"
                                     Grid.Row="1"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                        <!-- Панель для горизонтального расположения карточек -->
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" 
                                                   ItemWidth="180" 
                                                   ItemHeight="200"/>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>

                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <ContentPresenter/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>

                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#E0E0E0" 
                                                    BorderThickness="1" 
                                                    CornerRadius="5"
                                                    Background="White"
                                                    Margin="5"
                                                    Width="170" 
                                                    Height="190">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="100"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>
                                                        <Border Grid.Row="0" 
                                                    Background="#F5F5F5"
                                                    CornerRadius="3">

                                                            <Image Source="{Binding ProductVariation.ImagePath}" 
                                                       Stretch="Uniform"
                                                       MaxWidth="150"
                                                       MaxHeight="80"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                                        </Border>
                                                        <StackPanel Grid.Row="1" 
                                                                Margin="10,5">
                                                            <TextBlock Text="{Binding  ProductVariation.Product.Name}" 
                                                                FontWeight="Bold"
                                                                TextWrapping="Wrap"
                                                                MaxHeight="20"/>
                                                            <TextBlock Text="{Binding Quantity, StringFormat='Кол-во товара: {0}'}" 
                                                                        TextWrapping="Wrap"
                                                                        MaxHeight="20"/>
                                                            <StackPanel Margin="0,5,0,0">
                                                                <TextBlock Text="{Binding  ProductVariation.Description, StringFormat='Описание: {0}'}"
                                                            FontSize="11"/>
                                                                <TextBlock Text="{Binding  ProductVariation.Price, StringFormat='Цена: {0}₽'}"
                                                            FontSize="11"
                                                            Margin="0,2,0,0"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>