<Window x:Class="UI.DataGridWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="DataGridWindow" Height="450" Width="800">
    <TabControl Margin="0,60,0,0">
        <TabItem Header="Продукты">
            <Grid Background="#FFE5E5E5">
                <DataGrid x:Name="ProductsDataGrid" d:ItemsSource="{Binding ProductsRepository}"/>
            </Grid>
        </TabItem>
        <TabItem Header="Покупки">
            <Grid Background="#FFE5E5E5">
                <DataGrid x:Name="SaleDataGrid" d:ItemsSource="{Binding SaleRepository}"/>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
